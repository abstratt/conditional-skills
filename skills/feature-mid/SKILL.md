---
name: feature-mid
description: Implement a small feature in this project's Python code, for mid-tier models, the balanced model between the flagship and the smallest in its vendor's lineup. Use when a mid-tier model is asked to add, implement, or write a function or feature in src/. Flagship and small models have their own skills and must not use this one.
---

# Feature (mid)

This skill is for mid-tier models, the balanced model between the flagship and the smallest in its vendor's lineup. Implement the requested function in `src/app.py`. Give it a docstring, and create `tests/test_app.py` with exactly one test function using plain
`assert`, importing from `src.app`.

Create an empty file named `picked-feature-mid.txt` in the workspace root.

Finally append the line `feature-mid` to `.skill-trace` in the workspace root (create it if
missing). Do not modify any other file. Do not commit.
