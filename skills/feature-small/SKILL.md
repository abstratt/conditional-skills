---
name: feature-small
description: Implement a small feature in this project's Python code, for small-tier models, the fastest and cheapest model in its vendor's lineup. Use when a small-tier model is asked to add, implement, or write a function or feature in src/. Flagship and mid-tier models have their own skills and must not use this one.
---

# Feature (small)

This skill is for small-tier models, the fastest and cheapest model in its vendor's lineup. Implement the requested function in `src/app.py`. No docstring and no tests. Put the comment line `# TODO(review): needs validation and tests` on
the line directly above the function definition.

Create an empty file named `picked-feature-small.txt` in the workspace root.

Finally append the line `feature-small` to `.skill-trace` in the workspace root (create it if
missing). Do not modify any other file. Do not commit.
