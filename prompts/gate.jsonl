{"id": "GT1", "study": "gate", "seed": "seed-2", "skill": "tier-gated-guidance", "kind": "gated", "gate": {"tier": "small"}, "expect_trigger": true, "prompt": "Add a function slugify(text) to src/app.py that lowercases the text and replaces each run of whitespace with a single hyphen.", "python_check": "from src.app import slugify; assert slugify('Hello  World') == 'hello-world'; assert slugify('a') == 'a'"}
{"id": "GT2", "study": "gate", "seed": "seed-2", "skill": "tier-gated-guidance", "kind": "gated", "gate": {"tier": "small"}, "expect_trigger": true, "prompt": "Implement word_count(text) in src/app.py, returning the number of whitespace-separated words.", "python_check": "from src.app import word_count; assert word_count('a b  c') == 3; assert word_count('') == 0"}
{"id": "GT3", "study": "gate", "seed": "seed-2", "skill": "tier-gated-guidance", "kind": "gated", "gate": {"tier": "small"}, "expect_trigger": true, "prompt": "Add clamp(n, lo, hi) to src/app.py that returns n limited to the inclusive range lo..hi.", "python_check": "from src.app import clamp; assert clamp(5, 0, 3) == 3; assert clamp(-1, 0, 3) == 0; assert clamp(2, 0, 3) == 2"}
{"id": "GV1", "study": "gate", "seed": "seed-2", "skill": "vendor-gated-guidance", "kind": "gated", "gate": {"vendor": "anthropic"}, "expect_trigger": true, "prompt": "Add a function slugify(text) to src/app.py that lowercases the text and replaces each run of whitespace with a single hyphen.", "python_check": "from src.app import slugify; assert slugify('Hello  World') == 'hello-world'; assert slugify('a') == 'a'"}
{"id": "GV2", "study": "gate", "seed": "seed-2", "skill": "vendor-gated-guidance", "kind": "gated", "gate": {"vendor": "anthropic"}, "expect_trigger": true, "prompt": "Implement word_count(text) in src/app.py, returning the number of whitespace-separated words.", "python_check": "from src.app import word_count; assert word_count('a b  c') == 3; assert word_count('') == 0"}
{"id": "GV3", "study": "gate", "seed": "seed-2", "skill": "vendor-gated-guidance", "kind": "gated", "gate": {"vendor": "anthropic"}, "expect_trigger": true, "prompt": "Add clamp(n, lo, hi) to src/app.py that returns n limited to the inclusive range lo..hi.", "python_check": "from src.app import clamp; assert clamp(5, 0, 3) == 3; assert clamp(-1, 0, 3) == 0; assert clamp(2, 0, 3) == 2"}
{"id": "ST1", "study": "gate", "seed": "seed-2", "skill": "select-tier", "skills": ["feature-flagship", "feature-mid", "feature-small"], "kind": "selection", "select_by": "tier", "expect_trigger": true, "prompt": "Add a function slugify(text) to src/app.py that lowercases the text and replaces each run of whitespace with a single hyphen.", "python_check": "from src.app import slugify; assert slugify('Hello  World') == 'hello-world'; assert slugify('a') == 'a'"}
{"id": "ST2", "study": "gate", "seed": "seed-2", "skill": "select-tier", "skills": ["feature-flagship", "feature-mid", "feature-small"], "kind": "selection", "select_by": "tier", "expect_trigger": true, "prompt": "Implement word_count(text) in src/app.py, returning the number of whitespace-separated words.", "python_check": "from src.app import word_count; assert word_count('a b  c') == 3; assert word_count('') == 0"}
{"id": "ST3", "study": "gate", "seed": "seed-2", "skill": "select-tier", "skills": ["feature-flagship", "feature-mid", "feature-small"], "kind": "selection", "select_by": "tier", "expect_trigger": true, "prompt": "Add clamp(n, lo, hi) to src/app.py that returns n limited to the inclusive range lo..hi.", "python_check": "from src.app import clamp; assert clamp(5, 0, 3) == 3; assert clamp(-1, 0, 3) == 0; assert clamp(2, 0, 3) == 2"}
{"id": "SV1", "study": "gate", "seed": "seed-2", "skill": "select-vendor", "skills": ["feature-anthropic", "feature-openai"], "kind": "selection", "select_by": "vendor", "expect_trigger": true, "prompt": "Add a function slugify(text) to src/app.py that lowercases the text and replaces each run of whitespace with a single hyphen.", "python_check": "from src.app import slugify; assert slugify('Hello  World') == 'hello-world'; assert slugify('a') == 'a'"}
{"id": "SV2", "study": "gate", "seed": "seed-2", "skill": "select-vendor", "skills": ["feature-anthropic", "feature-openai"], "kind": "selection", "select_by": "vendor", "expect_trigger": true, "prompt": "Implement word_count(text) in src/app.py, returning the number of whitespace-separated words.", "python_check": "from src.app import word_count; assert word_count('a b  c') == 3; assert word_count('') == 0"}
{"id": "SV3", "study": "gate", "seed": "seed-2", "skill": "select-vendor", "skills": ["feature-anthropic", "feature-openai"], "kind": "selection", "select_by": "vendor", "expect_trigger": true, "prompt": "Add clamp(n, lo, hi) to src/app.py that returns n limited to the inclusive range lo..hi.", "python_check": "from src.app import clamp; assert clamp(5, 0, 3) == 3; assert clamp(-1, 0, 3) == 0; assert clamp(2, 0, 3) == 2"}
